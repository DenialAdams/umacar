[package]
name = "umacar"
version = "0.1.0"
edition = "2024"

[dependencies]
atomic_float = "1"
rand = "0.9"
noisy_float = "0.2"
num-traits = "0.2"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
rand_xorshift = "0.4"

[profile.release]
debug = true
panic = 'abort'